import { createStitches } from '@stitches/react';

export const { createTheme } = createStitches({
  theme: {
    colors: {
      primary: 'green',
      secondary: 'pink',
      other: 'orange',
    },
  },
});
