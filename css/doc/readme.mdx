import { mdx } from '@mdx-js/react';
import { MdxLayout } from '~/layout';
export default MdxLayout;

# Bench Design System CSS

Import this file into the consuming system to define styles required by the Bench Design System components.