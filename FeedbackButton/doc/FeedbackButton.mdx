import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { MdxButtonLayout } from '~/layout';
import FeedbackButton from '../src/FeedbackButton';
export default MdxButtonLayout;

# FeedbackButton
<Description of={FeedbackButton} />

### Inferring busy state
<p>
In the below usage, the busy indicator will be displayed while the promise is being executed, once completed, te button will display the success icon briefly to the user.
</p>
<Playground
  scope={{ FeedbackButton }}
  code={`
  <FeedbackButton
    variant="primary"
    inferBusy
    onClick={() => new Promise((resolve) => setTimeout(resolve, 3000))}
  >
    Click Me
  </FeedbackButton>
  `}
/>

### Notify Success onClick (Synchronous)
<p>
  Just like the promise-based version above, "onClick" can receive a synchronous callback that will be turned into a promise
  inside of the FeedbackButton. I think
</p>
<Playground
  scope={{ FeedbackButton }}
  code={`
  <FeedbackButton
    variant="primary"
    showOnClickResult
    onClick={() => {}}
  >
    Click Me
  </FeedbackButton>
  `}
/>


### Notify Failure onClick
<Playground
  scope={{ FeedbackButton }}
  code={`
  <FeedbackButton
    variant="primary"
    showOnClickResult
    onClick={() => new Promise((resolve, reject) => reject())}
  >
    Click Me
  </FeedbackButton>
  `}
/>


## Props
In addition to the below props, we also expose the props used with a native "FeedbackButton" element.

<Props of={FeedbackButton} />
