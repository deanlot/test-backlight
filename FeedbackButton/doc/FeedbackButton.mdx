import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { MdxFeedbackButtonLayout } from '~/layout';
import FeedbackFeedbackButton from '~/FeedbackButton/src/FeedbackFeedbackButton';
export default MdxFeedbackButtonLayout;

# FeedbackButton
<Description of={FeedbackButton} />

## Usage
### Notify Success onClick (Promise)
<p>
  "onClick" can receive either a promise or a synchronous callback. Below is an example of a promise-based callback.
  This will trigger a success icon to be displayed for 1200ms on resolution of the promise
</p>
<Playground
  scope={{ FeedbackButton }}
  code={`
  <FeedbackButton
    variant="primary"
    showOnClickResult
    onClick={() => new Promise((resolve) => resolve())}
  >
    Click Me
  </FeedbackButton>
  `}
/>

### Notify Success onClick (Synchronous)
<p>
  Just like the promise-based version above, "onClick" can receive a synchronous callback that will be turned into a promise
  inside of the FeedbackButton. I think
</p>
<Playground
  scope={{ FeedbackButton }}
  code={`
  <FeedbackButton
    variant="primary"
    showOnClickResult
    onClick={() => {}}
  >
    Click Me
  </FeedbackButton>
  `}
/>


### Notify Failure onClick
<Playground
  scope={{ FeedbackButton }}
  code={`
  <FeedbackButton
    variant="primary"
    showOnClickResult
    onClick={() => new Promise((resolve, reject) => reject())}
  >
    Click Me
  </FeedbackButton>
  `}
/>


## Props
In addition to the below props, we also expose the props used with a native "FeedbackButton" element.

<Props of={FeedbackButton} />
