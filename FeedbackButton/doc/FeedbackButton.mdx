import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { MdxButtonLayout } from '~/layout';
import { Button } from '~/Button/src/BaseButton';
export default MdxButtonLayout;

# Button
<Description of={Button} />

## Usage
### Primary Button
<Playground
  scope={{ Button }}
  code={`
<Button variant='primary'>Primary</Button>
  `}
/>

### Secondary Button
<Playground
  scope={{ Button }}
  code={`
<Button variant='secondary'>Secondary</Button>
  `}
/>

### Ghost Button
<Playground
  scope={{ Button }}
  code={`
<Button variant='ghost'>Ghost</Button>
  `}
/>

### Busy State
<Playground
  scope={{ Button }}
  code={`
<Button variant='primary' busy>Primary</Button>
  `}
/>

### Disabled State
<p>Note how "disabled" state always takes priority over "busy" state.</p>
<Playground
  scope={{ Button }}
  code={`
<Button variant='primary' disabled busy>Primary</Button>
  `}
/>

### Notify Success onClick (Promise)
<p>
  "onClick" can receive either a promise or a synchronous callback. Below is an example of a promise-based callback.
  This will trigger a success icon to be displayed for 1200ms on resolution of the promise
</p>
<Playground
  scope={{ Button }}
  code={`
  <Button
    variant="primary"
    showOnClickResult
    onClick={() => new Promise((resolve) => resolve())}
  >
    Click Me
  </Button>
  `}
/>

### Notify Success onClick (Synchronous)
<p>
  Just like the promise-based version above, "onClick" can receive a synchronous callback that will be turned into a promise
  inside of the button. I think
</p>
<Playground
  scope={{ Button }}
  code={`
  <Button
    variant="primary"
    showOnClickResult
    onClick={() => {}}
  >
    Click Me
  </Button>
  `}
/>


### Notify Failure onClick
<Playground
  scope={{ Button }}
  code={`
  <Button
    variant="primary"
    showOnClickResult
    onClick={() => new Promise((resolve, reject) => reject())}
  >
    Click Me
  </Button>
  `}
/>


## Props
In addition to the below props, we also expose the props used with a native "button" element.

<Props of={Button} />
