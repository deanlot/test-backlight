import { mdx } from '@mdx-js/react';
import { MdxLayout } from '~/layout';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import CheckIcon from '../icons/CheckIcon/src/CheckIcon';
import BenchIcon from '../icons/BenchIcon/src/BenchIcon';
import Button from '../../Button/src/Button'
import { BrandIcon, Icon } from '../src/IconVariants';
import IconPreview from './components/IconPreview';
import { ButtonPreviewContainer } from './components/ButtonPreviewContainer.styles'; import BlockSymbol
  from '../symbols/BlockSymbol/src/BlockSymbol'; import PendingSymbol from '../symbols/PendingSymbol/src/PendingSymbol'; import QASymbol
  from '../symbols/QASymbol/src/QASymbol'; import WarningSymbol from '../symbols/WarningSymbol/src/WarningSymbol'; import DoneSymbol
  from '../symbols/DoneSymbol/src/DoneSymbol';
export default MdxLayout;

# Icon
<Description of={Icon} />

## Icons
<IconPreview />

## Usage
### No Overridden Styling
Icons have default styling for SVG styling like fill, stroke, etc. These are defaulted to brand values when no extra props are passed in.
<Playground
  scope={{ CheckIcon }}
  code={`
<CheckIcon size="s" />
<CheckIcon size="m" />
<CheckIcon size="l" />
<CheckIcon size="xl"/>
  `}
/>

### Overridden Styling
Fill and color can be passed in as props.
<Playground
  scope={{ CheckIcon }}
  code={`
      <CheckIcon size="m" stroke="#F0BD6B" />
  `}
/>

### Usage With Other Components
The Icon components can be used inside other components as well, e.g. Button.
<Playground
  scope={{ CheckIcon, Button, ButtonPreviewContainer }}
  code={`
<ButtonPreviewContainer>
   <Button size="m" icon={<CheckIcon />} >Submit</Button>
   <Button size="m" icon={<CheckIcon />} iconPlacement="left" >Submit</Button>
   <Button size="m" icon={<CheckIcon />} />
</ButtonPreviewContainer>
`}
/>

### Brand Icon
<Description of={BrandIcon} />
<Playground
  scope={{ BenchIcon, CheckIcon }}
  code={`
/** Bench Icon */
<BenchIcon size="s" />
<BenchIcon size="m" />
<BenchIcon size="l" />
<BenchIcon size="xl"/>
/** Check Icon */
<CheckIcon size="s" />
<CheckIcon size="m" />
<CheckIcon size="l" />
<CheckIcon size="xl"/>
`}
/>

### Symbol Icons
These symbols are used within other components. Will add more description at a later date.
<Playground
  scope={{ BlockSymbol, PendingSymbol, QASymbol, WarningSymbol, DoneSymbol }}
  code={`
<BlockSymbol />
<PendingSymbol />
<QASymbol />
<WarningSymbol />
<DoneSymbol />
`}
/>


## Props
<Props of={CheckIcon} />
