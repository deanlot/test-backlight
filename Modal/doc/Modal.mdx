import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { MdxLayout } from '~/layout';
import Modal from '../src/Modal';
import Button from '~/Button/src/Button';
export default MdxLayout;


# Modal
<Description of={Modal} />

### Uncontrolled
<p>
  Uncontrolled modal with customised button.
</p>
<Playground
  scope={{ Modal, Button }}
  code={`
  <Modal
   defaultOpen={false}
   trigger={<Button variant="solid">Open me</Button>}
  >
    Here's some modal content
  </Modal>
  `}
/>

## Primary props
<Props of={Modal} />
