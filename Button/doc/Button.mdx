import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { MdxLayout } from '~/layout';
import Button from '../src/Button';
import { ThemePlayground } from '../../theme-playground';
export default MdxLayout;

# Button
<Description of={Button} />

## Usage
### Solid Button
<ThemePlayground
  scope={{ Button }}
  code={`
<Button>Solid</Button>
  `}
/>

### Outline Button
<ThemePlayground
  scope={{ Button }}
  code={`
<Button variant='outline'>Outline</Button>
  `}
/>

### Ghost Button
<ThemePlayground
  scope={{ Button }}
  code={`
<Button variant='ghost'>Ghost</Button>
  `}
/>

### Busy State
<ThemePlayground
  scope={{ Button }}
  code={`
<Button busy>Solid</Button>
  `}
/>

### Disabled State
<p>Note how "disabled" state always takes priority over "busy" state.</p>
<ThemePlayground
  scope={{ Button }}
  code={`
<Button disabled busy>Solid</Button>
  `}
/>


## Props
In addition to the below props, we also expose the props used with a native "button" element.

<Props of={Button} />
