import React from 'react';
import RadButton from '../src/RadButton';

export const secondary_disabled = () => (
  <>
    <RadButton variant={'secondary'}>Lekker</RadButton>
    <RadButton variant={'primary'}>PRIMARY</RadButton>
  </>
);
