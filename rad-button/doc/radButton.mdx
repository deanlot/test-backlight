import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { MdxButtonLayout } from '~/layout';
import {RadButton} from '../src/RadButton';
export default MdxButtonLayout;

# Button

<Description of={RadButton} />

## Usage

```tsx
import {RadButton} from '../src/RadButton';

<RadButton variant="primary">Primary</RadButton>
<RadButton variant="secondary">Secondary</RadButton>
<RadButton disabled>Disabled</RadButton>
```

## Example

<Playground
  scope={{ RadButton }}
  code={`
<RadButton variant="primary" >Primary</RadButton>
<RadButton variant="secondary" >Secondary</RadButton>
<RadButton disabled>Disabled</RadButton>
  `}
/>

## Props
In addition to the below props, we also expose the props used with a native "button" element.

<Props of={RadButton} />

<object
  width="300"
  height="300"
  src="https://benchco.atlassian.net/jira/projects"
></object>
