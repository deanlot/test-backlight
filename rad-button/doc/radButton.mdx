import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { MdxLayout } from '~/layout';
import { RadButton } from '../src/RadButton';
import ThemePlayground from '../../theme-playground/ThemePlayground';
export default MdxLayout;

# Button
<Description of={RadButton} />

## Usage
### Solid Button
<ThemePlayground
  scope={{ RadButton }}
  code={`
<RadButton variant='solid'>Solid</RadButton>
  `}
/>

### Outline Button
<ThemePlayground
  scope={{ RadButton }}
  code={`
<RadButton variant='outline'>Outline</RadButton>
  `}
/>

### Ghost Button
<ThemePlayground
  scope={{ RadButton }}
  code={`
<RadButton variant='ghost'>Ghost</RadButton>
  `}
/>

### Busy State
<ThemePlayground
  scope={{ RadButton }}
  code={`
<RadButton variant='solid' busy>Solid</RadButton>
  `}
/>

### Disabled State
<p>Note how "disabled" state always takes priority over "busy" state.</p>
<ThemePlayground
  scope={{ RadButton }}
  code={`
<RadButton variant='solid' disabled busy>Solid</RadButton>
  `}
/>

### Notify Success onClick (Promise)
<p>
  "onClick" can receive either a promise or a synchronous callback. Below is an example of a promise-based callback.
  This will trigger a success icon to be displayed for 1200ms on resolution of the promise
</p>
<ThemePlayground
  scope={{ RadButton }}
  code={`
  <RadButton
    variant="solid"
    showOnClickResult
    onClick={() => new Promise((resolve) => resolve())}
  >
    Click Me
  </RadButton>
  `}
/>

### Notify Success onClick (Synchronous)
<p>
  Just like the promise-based version above, "onClick" can receive a synchronous callback that will be turned into a promise
  inside of the button. I think
</p>
<ThemePlayground
  scope={{ RadButton }}
  code={`
  <RadButton
    variant="solid"
    showOnClickResult
    onClick={() => {}}
  >
    Click Me
  </RadButton>
  `}
/>


### Notify Failure onClick
<ThemePlayground
  scope={{ RadButton }}
  code={`
  <RadButton
    variant="solid"
    showOnClickResult
    onClick={() => new Promise((resolve, reject) => reject())}
  >
    Click Me
  </RadButton>
  `}
/>


## Props
In addition to the below props, we also expose the props used with a native "button" element.

<Props of={RadButton} />
