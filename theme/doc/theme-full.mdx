import { mdx } from '@mdx-js/react';
import { themeLight } from '../';
import { SystemUIShowcases } from '@divriots/dockit-react/systemui-showcases';
import { MdxLayout } from '~/layout';
export default MdxLayout;

# Theme

[Colors](#colors)

[Space](#space)

[Font Size](#fontSize)

[Font Family](#fontFamily)

[Font Weight](#fontWeight)

[Line Height](#lineHeight)

[Letter Spacing](#letterSpacing)

[Radii](#radii)




<span id="colors"/>

# Colors

<div>
    {
         Object.keys(themeLight.colors).map(colorGroup => {
            return <div key={colorGroup}>
                <h2 style={{ textTransform: "capitalize" }}>{colorGroup}</h2>
                {Object.keys(themeLight.colors[colorGroup]).map(color => {
                    return <div key={color} style={{ marginBottom: 32 }}>
                        <h4>{colorGroup}.{color}</h4>
                        <div style={{display: "flex", alignItems: "center", gap: 8, marginTop: 8}}>
                            <div style={{
                                backgroundColor: themeLight.colors[colorGroup][color],
                                width: 80,
                                height: 80,
                                boxShadow: '0 2px 8px rgba(149, 157, 165, 0.3)',
                                borderRadius: 4
                            }}/>
                            <div style={{ color: themeLight.colors[colorGroup][color], width: 200 }}>The quick brown fox jumps over the lazy dog.</div>
                        </div>
                    </div>
                })}
                <hr style={{ marginBottom: 48, marginTop: 48 }} />
            </div>
        }
    )}
</div>

<span id="space"/>

# Space

<div id="space">
    <SystemUIShowcases theme={themeLight} showcaseKey="space" />
</div>

<span id="fontSize"/>

# Font Size

<SystemUIShowcases theme={themeLight} showcaseKey="fontSize" />

<span id="fontFamily"/>

# Font Family

<SystemUIShowcases theme={themeLight} showcaseKey="fontFamily" />

<span id="fontWeight"/>

# Font Weight

<SystemUIShowcases theme={themeLight} showcaseKey="fontWeight" />

<span id="lineHeight"/>

# Line Height

<SystemUIShowcases theme={themeLight} showcaseKey="lineHeight" />

<span id="letterSpacing"/>

# Letter Spacing

<SystemUIShowcases theme={themeLight} showcaseKey="letterSpacing" />


<span id="radii"/>

# Radii

<div id="radii">
    <SystemUIShowcases
      theme={themeLight}
      showcaseKey="borderRadius"
      componentProps={{
        width: '8rem',
        height: '8rem',
        boxShadow: '0 12px 48px rgba(149, 157, 165, 0.3)',
        bg: '#22863a',
      }}
    />
</div>
