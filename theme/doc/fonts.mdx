import { mdx } from '@mdx-js/react';
import { themeLight as theme } from '../';
import { SystemUIShowcases } from '@divriots/dockit-react/systemui-showcases';
import { MdxLayout } from '~/layout';
export default MdxLayout;

# Fonts

{
  Object.keys(theme.fonts).map(token => (
    <div style={{ display: "flex", marginBottom: "2rem", marginRight: "2rem" }}>
      <div style={{ width: "5.5rem", fontFamily: "monospace" }}>{token}</div>
      <div style={{ fontFamily: theme.fonts[token] }}>{theme.fonts[token]}</div>
    </div>)
  )
}
